import {expect} from 'chai';
import mixins from '../../src/index';

describe('mixins', function () {
  describe('mixins', function () {
    it('should include all the mixins', function () {
      expect(mixins).to.not.eql({});
    });
  });
});